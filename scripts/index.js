const sns = new Swiper('.disign',{
    autoplay:{delay:0,},
    speed:6000,
    loop:true,
})
const banner = new Swiper('.banner',{
    autoplay:{delay:0,},
    speed:6000,
    loop:true,
})
const detail = new Swiper('.detail',{
    autoplay:{delay:0,},
    speed:5000,
    loop:true,
})